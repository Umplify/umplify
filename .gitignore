# Vim
*~
*.sw[p_]

# Sublime Text
*.sublime-project
*.sublime-workspace

# Ruby Gem
*.gem
.bundle
Gemfile.lock
**/vendor/bundle

# Node.js and NPM
node_modules
npm-debug.log*
package-lock.json
codekit-config.json

# macOS
.DS_Store

# Jekyll generated files
.jekyll-cache
.jekyll-metadata
.sass-cache
_asset_bundler_cache
_site

# Internal planning docs: contain Azure resource identifiers and client
# references. Keep local or move to the private umplify-forms repo.
docs/superpowers/
Gemfile.preview
Gemfile.preview.lock
_config.preview.yml
.playwright-mcp/

# Marketing creative. Kept local and out of the repository by the owner's
# decision: this repo is public, and the campaign artwork is managed
# elsewhere rather than versioned here.
assets/images/marketing/
